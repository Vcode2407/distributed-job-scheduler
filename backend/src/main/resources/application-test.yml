spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  kafka:
    consumer:
      auto-offset-reset: earliest

app:
  security:
    jwt-secret: test-secret-test-secret-test-secret-1234
    dev-token-enabled: true
    rate-limit:
      capacity: 10000
      refill-per-minute: 10000
